apiVersion: v1
kind: Service
metadata:
  name: training-planner
  namespace: training-planner
  labels:
    app.kubernetes.io/name: training-planner
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: training-planner
  ports:
    - name: http
      port: 3000
      targetPort: http
