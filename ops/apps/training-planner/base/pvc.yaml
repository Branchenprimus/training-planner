apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: training-planner-data
  namespace: training-planner
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 2Gi
